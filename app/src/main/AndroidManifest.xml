<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetoEpagri">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--Activity Principal-->
        <activity android:name="com.example.projetoEpagri.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Activity Perfil-->
        <activity
            android:name="com.example.projetoEpagri.Perfil"
            android:label=" Criar Perfil"
            android:parentActivityName="com.example.projetoEpagri.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.projetoEpagri.MainActivity" />
        </activity>

        <!--Activity De lista de usuarios-->
        <activity android:name="com.example.projetoEpagri.ListaUsuarios"
            android:label="Lista de Usuarios"
            android:parentActivityName="com.example.projetoEpagri.Perfil">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.projetoEpagri.Perfil" />
        </activity>


    </application>

</manifest>